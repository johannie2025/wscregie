<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/bg_deep">

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="vertical" android:padding="16dp">

        <!-- Header -->
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:gravity="center_vertical"
            android:layout_marginBottom="16dp">
            <ImageButton android:id="@+id/btn_back_admin" android:layout_width="40dp"
                android:layout_height="40dp" android:src="@drawable/ic_admin"
                android:tint="@color/accent_gold" android:background="@android:color/transparent"/>
            <TextView android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="Panneau Administrateur"
                android:textColor="@color/accent_gold" android:textSize="18sp"
                android:textStyle="bold" android:layout_marginStart="10dp"/>
        </LinearLayout>

        <!-- Info admin -->
        <TextView android:id="@+id/tv_admin_info"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textSize="12sp" android:textColor="@color/text_light"
            android:background="@drawable/card_bg" android:padding="12dp"
            android:layout_marginBottom="16dp"/>

        <!-- Durée licence -->
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="DURÉE DE LICENCE" android:textSize="10sp"
            android:textColor="@color/text_dim" android:letterSpacing="0.1"
            android:layout_marginBottom="8dp"/>

        <RadioGroup android:id="@+id/rg_duration"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="horizontal" android:layout_marginBottom="16dp">
            <RadioButton android:id="@+id/rb_90"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="90 jours"
                android:textColor="@color/text_white" android:checked="true"/>
            <RadioButton android:id="@+id/rb_180"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="180 jours"
                android:textColor="@color/text_white"/>
            <RadioButton android:id="@+id/rb_365"
                android:layout_width="0dp" android:layout_height="wrap_content"
                android:layout_weight="1" android:text="365 jours"
                android:textColor="@color/accent_gold"/>
        </RadioGroup>

        <!-- Quantité -->
        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="QUANTITÉ (1–10)" android:textSize="10sp"
            android:textColor="@color/text_dim" android:letterSpacing="0.1"
            android:layout_marginBottom="8dp"/>

        <NumberPicker android:id="@+id/np_count"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"/>

        <!-- Bouton Générer -->
        <Button android:id="@+id/btn_generate"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:text="⚡ Générer les licences"
            android:textStyle="bold" android:textSize="15sp"
            android:background="@drawable/btn_primary_bg"
            android:textColor="#FFFFFF" android:layout_marginBottom="12dp"/>

        <!-- Bouton Tout copier -->
        <Button android:id="@+id/btn_copy_all"
            android:layout_width="match_parent" android:layout_height="42dp"
            android:text="📋 Tout copier"
            android:textSize="13sp"
            android:background="@drawable/btn_outline_bg"
            android:textColor="@color/text_light"
            android:layout_marginBottom="12dp"
            android:visibility="gone"/>

        <!-- Clés générées -->
        <LinearLayout android:id="@+id/generated_keys"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:orientation="vertical"/>

    </LinearLayout>
</ScrollView>
